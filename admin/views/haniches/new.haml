.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:haniches, :index)
      %li.active=link_to pat(:new), url(:haniches, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:hanich)
    .inner
      -form_for :hanich, url(:haniches, :create), :class => :form do |f|
        =partial "haniches/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")